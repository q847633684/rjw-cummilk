---
description: 在本项目中优先使用记忆库（记忆库/）作为设计、约定与领域知识的来源与沉淀目标
alwaysApply: true
---

# 使用记忆库

在本项目（rjw-cummilk）中，**记忆库/** 是长记忆的权威位置。回答涉及设计、约定或领域知识时，应优先读取并酌情写入记忆库。

## 何时读取记忆库

- **设计/实现**：讨论「为什么这样实现」、与规格对应时 → 读 `记忆库/design/`、`记忆库/index.md`。
- **规范/风格**：命名空间、EMDefOf、翻译、Harmony、设置时 → 读 `记忆库/conventions/`；可与 `.cursor/skills/rjw-cummilk-source` 配合。
- **领域术语**：L、双池、E_tol、吸收延迟、进水/衰减公式等 → 读 `记忆库/domain/`；完整公式仍以 `Docs/泌乳系统逻辑图.md` 为准。
- **关键决策**：架构或兼容性相关选择 → 读 `记忆库/decisions/`。

**入口**：先看 `记忆库/README.md`、`记忆库/index.md` 定位类型与条目；需要完整 schema 时读 `记忆库/schema.md`、`记忆库/架构说明.md`。

## 何时写入记忆库

- 得出**可复用的设计结论**（如双池按对分组的原因）→ 在 `记忆库/design/` 新增或更新条目，并在 `记忆库/index.md` 的 design 节登记。
- **约定变更**或需要固定下来的规范摘要 → 更新 `记忆库/conventions/` 或对应 00-概览，必要时同步 skills。
- **新领域概念**或术语定稿 → 在 `记忆库/domain/` 新增条目，并在 index 登记。
- **重要决策**（影响架构/兼容性）→ 在 `记忆库/decisions/` 用 ADR 风格写一条，并在 index 登记。
- **修改与 L/双池/耐受/进水/衰减/选侧/吸收延迟相关的逻辑时**，顺带检查并更新记忆库中已有相关条目，避免记忆与代码脱节。

**约定**：一条记忆一个主题；不复制大段 Docs，用「见 Docs/xxx 第 N 节」引用；新增条目时在 `记忆库/index.md` 对应类型下补链接与简短摘要。

## 与 Docs / skills 的关系

- **记忆库**：摘要、索引、决策记录；详细规格与公式仍在 `Docs/`。
- **Skills**：操作规范（如何写代码、如何检查）；记忆库侧重「是什么」「为什么」。
- 代码注释可写「见 记忆库/design/xxx」或「见 Docs/泌乳系统逻辑图 第 N 节」。
