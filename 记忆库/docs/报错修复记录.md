# 报错修复记录

> **使用说明**：每次收到报错先查本文件，看是否属于重复修复（同一处改过又改回）；若是重复，需深挖根因再改。

---

## 记录格式（每条）

- **日期**：YYYY-MM-DD
- **报错摘要**：（一句话）
- **位置**：文件/行或组件
- **修复**：做了什么改动
- **重复？**：是/否；若「是」→ 见下方「重复修复·根因分析」

---

## 历史记录

- **日期**：2025-03-01
- **报错摘要**：CS0117 HediffSet 未包含 "Remove" 的定义
- **位置**：`Source/MilkCum/Milk/HarmonyPatches/Milking.cs` 第 152 行
- **修复**：Harmony 目标改为 `Pawn_HealthTracker.RemoveHediff`（RimWorld 中移除 hediff 的 API 在 Pawn_HealthTracker 上，不是 HediffSet）；Postfix 参数改为 `Pawn_HealthTracker __instance`，通过 `__instance.Pawn` 取 Pawn（RimWorld API 为 PascalCase）。
- **重复？**：否

- **日期**：2025-03-01
- **报错摘要**：CS1061 Pawn_HealthTracker 未包含 "pawn" 的定义
- **位置**：`Source/MilkCum/Milk/HarmonyPatches/Milking.cs` 第 159 行
- **修复**：`__instance.pawn` 改为 `__instance.Pawn`（RimWorld 类型公开成员为 PascalCase）。
- **重复？**：否（与上条同一次修复的跟进，属同一根因：未查原版 API 命名）

- **日期**：2025-03-01
- **报错摘要**：CS1061 Pawn_HealthTracker 未包含 "Pawn" 的定义
- **位置**：`Source/MilkCum/Milk/HarmonyPatches/Milking.cs` 第 159 行
- **修复**：不再从 `Pawn_HealthTracker` 取 Pawn（原版该类型可能不暴露或命名不同）；改为从参数 `hediff.pawn` 取所属 Pawn（Verse 中 Hediff 有 `pawn` 属性）。
- **重复？**：是 → 见下方根因分析

---

## 重复修复 · 根因分析

- **同一处**：`HediffSet_Remove_Patch` 中「如何拿到被移除 hediff 所属的 Pawn」。
- **根因**：Patch 的是 RimWorld 原版类型（`HediffSet`/`Pawn_HealthTracker`），未直接引用游戏程序集时无法确认其公开成员名（`Remove`/`pawn`/`Pawn` 均猜错）。
- **最终方案**：不依赖 `Pawn_HealthTracker` 的成员，直接使用 Postfix 已有的 `hediff` 参数，用 **`hediff.pawn`** 取所属 Pawn，避免依赖原版内部 API 命名。

- **日期**：2025-03-01
- **报错摘要**：MilkCum Patch failed for ITab_Pawn_Feeding_Patch — Patching exception in DrawRow(baby, RectDivider& row, feeder, mother, father, surrogate)
- **位置**：`Source/MilkCum/Milk/HarmonyPatches/Breastfeed.cs`，对 `ITab_Pawn_Feeding.DrawRow` 的 Transpiler
- **修复**：DrawRow 拆到单独 Patch 类，用 `TargetMethod()` 仅当方法为旧版 3 参数 (baby, row, feeder) 时才返回方法并打补丁；当前 1.6 为 6 参数 (baby, RectDivider&, feeder, mother, father, surrogate) 时返回 null 不 patch，避免运行时 Patching exception，喂食标签仍为原版显示。
- **重复？**：否

- **日期**：2025-03-01
- **报错摘要**：MilkCum Patch failed — Patching exception in ITab_Pawn_Feeding_DrawRow_Patch::TargetMethod()
- **位置**：`Source/MilkCum/Milk/HarmonyPatches/Breastfeed.cs`，DrawRow 的 TargetMethod()
- **修复**：TargetMethod 内用 try-catch 包裹全部逻辑，异常时 return null，避免静态构造抛错导致 mod 无法加载；并用显式 BindingFlags 替代 AccessTools.all，避免运行环境差异。
- **重复？**：否（与上条同一处 patch 的跟进修复）

- **日期**：2025-03-01
- **报错摘要**：Critical error while injecting translations into defs: System.ArgumentException: Invalid generic arguments (MakeGenericType / GenGeneric.MethodOnGenericType)
- **位置**：Verse.DefInjectionPackage.InjectIntoDefs（游戏核心），在 LoadedLanguage.InjectIntoData_BeforeImpliedDefs 时触发
- **修复**：堆栈未标明具体 mod/Def 类型，无法直接改代码修复。可能原因：某 mod 的 DefInjected 文件夹名对应的 Def 类型在当前游戏/DLC 下不存在或不符合泛型约束（例如 GeneDef 需 Biotech、类型名拼写错误等）。排查建议：① 临时禁用 rjw-cummilk 的 Languages 或仅保留 English 再进游戏，看是否仍报错；② 若消失则可能是本 mod 的 DefInjected 导致，本 mod 使用的 Def 类型有：NeedDef, HediffDef, PawnColumnDef, ThingDef, ThoughtDef, TraitDef, DesignatorDropdownGroupDef, DesignationCategoryDef, ChemicalDef, **GeneDef**, StatDef（其中 GeneDef 依赖 Biotech DLC）；③ 若确认为本 mod 且无 Biotech，可尝试临时移除或重命名 `Languages/ChineseSimplified/DefInjected/GeneDef` 与 `Languages/English/DefInjected/GeneDef` 再试，移除后基因描述会使用 Def 内英文。
- **重复？**：否

- **日期**：2025-03-01
- **报错摘要**：Could not resolve cross-reference: No RimWorld.StatDef named DrugDurationMultiplier found to give to RimWorld.StatModifier (null stat)
- **位置**：`Defs/HediffDefs.xml` 中 EM_Prolactin_Tolerance 的 stages/statFactors 引用
- **修复**：在 `Defs/MilkProductionStats.xml` 中新增 StatDef `DrugDurationMultiplier`（defaultBaseValue 1.0，category PawnGeneral），使交叉引用可解析；原版可能无此 defName 或来自 DLC，本 mod 自备后耐受阶段对药物持续时间的倍率 (0.9/0.7) 仍可生效（若游戏逻辑读取该 stat）。
- **重复？**：否

- **日期**：2025-03-01
- **报错摘要**：Could not resolve cross-reference: No RimWorld.JoyKindDef named Medical found to give to RimWorld.IngestibleProperties
- **位置**：`Defs/ThingDefs/Drug_FluidBuff.xml` 中 Cumpilation_Galactogogues 的 ingestible.joyKind
- **修复**：joyKind 从 Medical 改为 Chemical（原版无 JoyKindDef.Medical，Chemical 为成瘾品常用且本 mod LactatingItems 已用）。
- **重复？**：否

- **日期**：2025-03-01
- **报错摘要**：Critical error while injecting translations into defs: Invalid generic arguments（InjectIntoData_**AfterImpliedDefs** 阶段）
- **位置**：Verse.DefInjectionPackage.InjectIntoDefs，LoadedLanguage.InjectIntoData_AfterImpliedDefs
- **修复**：移除本 mod 对 GeneDef 的 DefInjected 注入，避免游戏在注入阶段解析 GeneDef 类型时触发 MakeGenericType 异常。具体：① 在 `Defs/GeneDefs.xml` 中为三个基因补全 `<description>`（英文）；② 删除 `Languages/English/DefInjected/GeneDef/Desc.xml` 与 `Languages/ChineseSimplified/DefInjected/GeneDef/Desc.xml`；③ 中文基因描述已放入 `Languages/ChineseSimplified/Keyed/Keyed.xml`（EM_Gene_*_description），供日后若需代码侧显示时使用。游戏内基因说明现统一显示 Def 内英文 description；若仍报错则可能来自其他 mod 的 DefInjected。若报错仍出现，请手动删除空目录 `Languages/English/DefInjected/GeneDef` 与 `Languages/ChineseSimplified/DefInjected/GeneDef`（游戏可能仍按文件夹名解析类型）。
- **重复？**：是（与前述 BeforeImpliedDefs 同属翻译注入 Def 的 Invalid generic arguments，本次落实 GeneDef 移除）

- **日期**：2025-03-01
- **报错摘要**：MilkCum Patch failed — ITab_Pawn_Feeding_DrawRow_Patch::TargetMethod() 与 HealthCardUtility_GetTooltip_BreastPool_Patch::GetTooltip 的 Patching exception
- **位置**：Breastfeed.cs 的 DrawRow patch 类；Milking.cs 的 HealthCardUtility GetTooltip patch
- **修复**：① 完全移除 ITab_Pawn_Feeding_DrawRow_Patch 类（TargetMethod 返回 null 时部分 Harmony 会报 unexpected result，故不再注册该 patch）。② HealthCardUtility_GetTooltip_BreastPool_Patch 改为显式指定重载 GetTooltip(Pawn, BodyPartRecord)，Postfix 签名改为 (ref string __result, Pawn pawn, BodyPartRecord part)，在方法内用 pawn.health.hediffSet.hediffs.Where(h => h.Part == part) 取得 diffs，避免与 (IEnumerable<Hediff>, Pawn, BodyPartRecord) 等重载混淆导致 Patching exception。
- **重复？**：否

- **日期**：2025-03-01
- **报错摘要**：吸收延迟显示 53686.1 小时（数值崩溃）
- **位置**：HediffComp_AbsorptionDelayTip 悬停显示剩余时间；根因在 WorldComponent_EqualMilkingAbsorptionDelay.GetAbsorptionDelayTicks / GetMetabolicRate
- **修复**：根因为代谢率≤0 或 NaN（基因减 100% metabolism、hediff/种族设 0、变种种族等）导致 delay = BaseAbsorptionDelayTicks/rate 爆炸。① GetMetabolicRate：stat 为 null 或 GetStatValue 结果为 NaN/Infinity/≤0 时统一返回 1f。② GetAbsorptionDelayTicks：对 rate 做 NaN/Infinity/≤0 保护后 Clamp(0.25f, 2f)，且返回值上限 2 游戏日（Mathf.Min(ticks, GenDate.TicksPerDay*2)），避免任何情况下返回数十万 tick。已撤回此前“显示层 cap / NormalizeAbsurdEndTicksAfterLoad”的修法。
- **重复？**：否

---
