# 未使用代码与函数清单

本条目记录 rjw-cummilk Source 内经引用扫描后**当前无任何调用**或**仅写入无读取**的成员。排除：RimWorld/Verse 重写、Harmony 目标、Def 指定类型、反射入口。

---

## 1. 当前未使用 / 仅写未读的成员

| 位置 | 成员 | 说明 |
|------|------|------|
| `CompEquallyMilkable.cs` | **Gathered(Pawn doer)**（方法） | 本 mod 内**无调用**。唯一调用方：RJW 的 `JobDriver_GatherHumanBodyResources`（1.1/1.2 的 tickAction）。方法体仅 `SyncBaseFullness()`，无产奶效果；保留为 RJW 兼容接口。 |

**已移除**：`assignedFeeders` 与 `allowedSucklers` 语义相同（谁可吸/喂此产奶者的奶），仅后者被读取；已删除字段、Scribe（"CanBeFedBy"）及 EnsureSaveCompatAllowedLists 中的维护。

---

## 2. 已删除的未使用成员（已从代码中移除）

| 位置 | 成员 | 说明 |
|------|------|------|
| `PawnMilkStateExtensions.cs` | `HasEdibleMilk` / `HasNutritiousMilk` / `HasDrugMilk` | 已删除（仅互相引用，无外部调用） |
| `Lang.cs` | `Lang.Spot`、`Lang.SpotDesc` | 已删除（无引用） |
| `Lang.cs` | `Of(this string noun, string adj)` | 已删除（无引用） |

---

## 3. 已确认有使用的常见成员（非未使用）

- **CompEquallyMilkable**：`AllowedToBeAutoFedBy(pawn)` ← `MilkPermissionExtensions` 两处；`LastGatheredTick` ← `ThoughtWorker_LongTimeNotMilked`；`OverflowAccumulator` ← `HediffWithComps_MilkCumLactating.CompDebugString`；`allowedSucklers` / `allowedConsumers` ← `MilkPermissionExtensions`、`CompProperties_ShowProducer`；`ClearPools`、`SetFullness`、`GetReabsorbedNutritionPerDay`、`HasTorsoOrBreastInjury`、`EnsureSaveCompatAllowedLists` 等见 MilkingPatch、Hediff、MilkCumSettings 等。
- **ChildcareHelper**：`AddBreastfeedActions`（Toil 扩展）← 本类两处；`IsMomsFault(BreastfeedFailReason?)` ← `JobGiver_MilkCumBreastfeed`。
- **基类重写**：`ResourceAmount`、`GatherResourcesIntervalDays`、`fResourceAmount` 为 `CompMilkable` 重写，由基类/原版逻辑调用，不视为未使用。
- `MilkCumSettings.IsMilkable(string)`：被 `IsMilkable(Pawn)` 内部调用。
- `MilkCumSettings.ApplyDefaultsFromDef`：`Widget_AdvancedSettings` 两处调用。
- `ListerAsync.AllColonyPawns` / `IsColonyPawn`：VCEIntegration、CompEquallyMilkable、JobGiver 等使用。
- `Lang` 其余静态属性和 `Label(HungerCategory)`、`GiveTo`、`DisplayText`、`SameWordsIn`、`MilkTypeDesc`、`MilkAmountDesc`、`StatFactor`、`Container` 等均有引用。

---

## 4. 扫描方法说明

- 对 private/internal 与 public 方法、字段在 `Source` 下做 grep 引用检查（排除定义处）。
- 未做：跨程序集、反射、Def 的 type 字段、Harmony 通过名字串调用的完整枚举。

**更新**：修改或新增公开 API 后，可复查本清单并更新「当前未使用」与「已确认有使用」两节。
